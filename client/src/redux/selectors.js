export const getCountries = (store) => store.countries;

/*export const getTodosByVisibilityFilter = (store, visibilityFilter) => {
  const allTodos = getTodos(store);
  // return allTodos.filter((todo) => todo.completed);
  // return allTodos;
};*/
